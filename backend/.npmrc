; npm's peer resolver crashes on vitest 4's optional peer graph
; (TypeError: Cannot read properties of null (reading 'edgesOut')).
; Pinning this here keeps `npm install` working for everyone and in CI.
legacy-peer-deps=true
