org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator.
# Must stay false: the google-services plugin (4.3.15) reads
# `applicationVariants`, which AGP 9's new ApplicationExtension no longer
# exposes. Keeping the legacy DSL means app/build.gradle.kts has to suppress
# the BaseAppModuleExtension deprecation — see the @file:Suppress there.
android.newDsl=false
